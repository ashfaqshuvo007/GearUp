enum ActiveStatus {
    ACTIVE
    SUSPENDED
    DRAFT
}

enum OrderStatus {
    PENDING
    CONFIRMED
    PICKEDUP
    RETURNED
    CANCELLED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}

enum Role {
    CUSTOMER
    PROVIDER
    ADMIN
}
